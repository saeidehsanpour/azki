@page
@model PerformanceManagementSystem.Areas.Admin.Pages.Positions.CreateModel

@{
    ViewData["Title"] = "ثبت مقام جدید";
}

<h1>ایجاد</h1>

<h4>مقام جدید</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="Position.OrganizationId" type="hidden" />
            <div class="form-group">
                <label asp-for="Position.Title" class="control-label"></label>
                <input asp-for="Position.Title" class="form-control" />
                <span asp-validation-for="Position.Title" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Position.Active" /> @Html.DisplayNameFor(model => model.Position.Active)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="ثبت" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index" asp-route-organizationId="@Model.Position.OrganizationId">بازگشت به لیست</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
